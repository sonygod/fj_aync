current version 0.1
useage:
	 hello = new Forwarder(cnx, "hello", HelloService.getInstance());

       hello.sayHello("hi", "god", onCalljs);

how to debug js file? run in debugger model  and FD auto create yourjs.js.map at the same path with your js file 
install chrome dev 28.0.1464.0 
http://dl.google.com/chrome/win/28.0.1464.0_chrome_installer.exe
and  select enable  Enable source maps in the dev setting panel 
and the Indentation choose 4 default